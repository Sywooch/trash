# DO NOT EDIT THIS FILE - edit the master and reinstall.
# (/tmp/crontab.5w7XKq/crontab installed on Mon May 12 17:23:23 2014)
# (Cron version -- $Id: crontab.c,v 2.13 1994/01/17 03:20:37 vixie Exp $)
# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command
SHELL=/bin/bash
PATH="/var/local/php/bin:/var/local/php/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:$PATH"
PRODUCTION_ROOT=/var/www/likefifa.ru

# Отправка сообщений по СМС
*/1 * * * * likefifa $PRODUCTION_ROOT/protected/yiic smssender >> $PRODUCTION_ROOT/protected/runtime/smssender-out.log 2>&1

# Уведомление через 12 часов после встречи
*/1 * * * * likefifa $PRODUCTION_ROOT/protected/yiic smsafter12sender >> $PRODUCTION_ROOT/protected/runtime/smsafter12sender-out.log 2>&1

# Уведомление о низском балансе, каждый день в 12 часов
0 12 * * * likefifa $PRODUCTION_ROOT/protected/yiic sms12sender >> $PRODUCTION_ROOT/protected/runtime/sms12sender-out.log 2>&1

# Автоматическое формирование рейтинга у мастеров
18 2 * * * likefifa $PRODUCTION_ROOT/protected/yiic updateRating >> $PRODUCTION_ROOT/protected/runtime/cron-updateRating-out.log 2>&1

# Пересчёт лайков фотографий
#18 3 * * * likefifa $PRODUCTION_ROOT/protected/yiic imagelikes >> $PRODUCTION_ROOT/protected/runtime/cron-imagelikes-out.log 2>&1

# Рандомная сортировка работ
1 * * * * likefifa $PRODUCTION_ROOT/protected/yiic works sorting

# Обновление индекса специализаций и услуг
*/10 * * * * likefifa $PRODUCTION_ROOT/protected/yiic sphinx suggest

# Уведомления
*/1 * * * * likefifa $PRODUCTION_ROOT/protected/yiic notification >> $PRODUCTION_ROOT/protected/runtime/notification-out.log 2>&1

# Рассылка мастерам через сутки после публикации
0 */1 * * * likefifa $PRODUCTION_ROOT/protected/yiic mailerStatus

# gzip
*/30 * * * * likefifa $PRODUCTION_ROOT/protected/yiic gzip
